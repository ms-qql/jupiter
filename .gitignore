# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Env / Secrets
.env
.env.*
!.env.example

# Node / Next.js
node_modules/
.next/
out/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build / Coverage
dist/
build/
coverage/
*.log

# OS / Editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# CodeGraph
.codegraph/

# PROJ-10: Laufzeit-Trust-Policy (nutzer-editiert, live) — nicht versionieren.
backend/config/policy.yaml

# PROJ-16: Laufzeit-Watchdog-Limits (nutzer-editiert, live) — nicht versionieren.
backend/config/watchdog.yaml

# PROJ-18: Laufzeit-Engine-Registry (deployment-lokal, box-spezifische URLs) — nicht versionieren.
backend/config/engines.yaml
